{
  // Vercel configuration for Static Buffet
  // Rewrites all requests to index.html so that client-side routing works correctly.
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}